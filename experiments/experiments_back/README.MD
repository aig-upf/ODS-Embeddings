This folder is meant to contain experimental traces, so that we can analyze results in terms of:

  * Timing.
  * Model performance e.g. for link prediction.
  * Logged execution errors e.g. out of memory failures.

